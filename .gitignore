# original client photos in the repo root stay local
*.jpg
*.webp
MATERIALS-SPEC.md

# except the sheet images the app links to from its examples
!app/sheets/*.jpg
!app/sheets/*.webp

# local tooling
.claude/
# old demo kept only as local reference
fence-calculator.html
